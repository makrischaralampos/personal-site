<div class="parallax-bg" aria-hidden="true">
  <div class="stars stars-small"></div>
  <div class="stars stars-medium"></div>
  <div class="stars stars-large"></div>
</div>

<style>
  .parallax-bg {
    position: fixed;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    background: var(--color-background);
  }

  .stars {
    position: absolute;
    inset: 0;
    background-repeat: repeat;
  }

  .stars-small {
    background-image:
      radial-gradient(1px 1px at 20px 30px, var(--color-fg4), transparent),
      radial-gradient(1px 1px at 90px 80px, var(--color-fg4), transparent),
      radial-gradient(1px 1px at 150px 40px, var(--color-fg4), transparent);
    background-size: 200px 200px;
    animation: drift 120s linear infinite;
    opacity: 0.4;
  }

  .stars-medium {
    background-image:
      radial-gradient(1.5px 1.5px at 50px 60px, var(--color-fg3), transparent),
      radial-gradient(1.5px 1.5px at 130px 20px, var(--color-fg3), transparent);
    background-size: 300px 300px;
    animation: drift 80s linear infinite;
    opacity: 0.3;
  }

  .stars-large {
    background-image:
      radial-gradient(2px 2px at 100px 100px, var(--color-yellow), transparent),
      radial-gradient(2px 2px at 200px 50px, var(--color-orange), transparent);
    background-size: 400px 400px;
    animation: drift 50s linear infinite;
    opacity: 0.2;
  }

  @keyframes drift {
    from {
      transform: translateY(0);
    }
    to {
      transform: translateY(-400px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .stars {
      animation: none;
    }
  }
</style>
